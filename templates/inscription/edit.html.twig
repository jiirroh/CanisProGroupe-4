{% extends 'base.html.twig' %}

{% block title %}Edit Inscription{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
{% endblock %}

{% block body %}
<div class="admin-container">
    <div class="admin-header">
    <h1>Modifier les reservations</h1>
    
    <a href="{{ path('app_inscription_index') }}" class="btn btn-ghost btn-sm">← Retour</a>
</div>
{{ include('inscription/_form.html.twig', {'button_label': 'Update'}) }}
    {{ include('inscription/_delete_form.html.twig') }}
</div>
{% endblock %}
