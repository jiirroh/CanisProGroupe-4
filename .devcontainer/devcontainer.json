{
    "name": "CanisPro PHP 8.4 Env",
    "image": "mcr.microsoft.com/devcontainers/php:8.4",
    "workspaceFolder": "/workspaces/CanisProGroupe-4",
    "features": {
        "ghcr.io/devcontainers/features/docker-in-docker:2": {}
    },
    "postCreateCommand": "docker compose up -d && docker compose exec web composer install && docker compose exec web php bin/console doctrine:migrations:migrate --no-interaction",
    "remoteUser": "root"
}
